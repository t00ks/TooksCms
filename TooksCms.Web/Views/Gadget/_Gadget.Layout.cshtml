@model TooksCms.Web.Models.GadgetViewModel

<section id="widget-@Model.Name" class="widget">
    <div class="head">
        <h3>@Model.Title</h3>
    </div>
    <div class="content">
        @{
            Html.RenderPartial(Model.InnerView, Model.Model);
        }
    </div>
</section>
<div style="clear:both;"></div>
